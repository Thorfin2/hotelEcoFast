{% extends 'admin/base.html.twig' %}

{% block title %}Test email SMTP{% endblock %}
{% block page_title %}Test email SMTP{% endblock %}
{% block page_subtitle %}<p class="text-dark-400 text-sm mt-0.5">Envoie un email de test pour verifier la configuration SMTP</p>{% endblock %}

{% block content %}
<div class="max-w-lg">
  <form method="post" class="card p-6 space-y-4">
    <div>
      <label for="to" class="block text-sm font-semibold text-dark-700 mb-1">Destinataire</label>
      <input type="email" id="to" name="to" value="{{ to }}" required
        class="form-input" placeholder="exemple@gmail.com">
    </div>
    <button type="submit" class="btn-gold w-full justify-center py-3">
      Envoyer le test
    </button>
  </form>

  {% if sent %}
    <div class="mt-6 rounded-lg border border-green-200 bg-green-50 text-green-800 px-4 py-3 text-sm">
      Email envoye avec succes ! Verifie la boite de reception (et les spams).
    </div>
  {% elseif error %}
    <div class="mt-6 rounded-lg border border-red-200 bg-red-50 text-red-800 px-4 py-3 text-sm whitespace-pre-wrap">
      <strong>Erreur :</strong> {{ error }}
    </div>
  {% endif %}

  <div class="mt-8 card p-4 text-xs text-dark-600 space-y-1">
    <p class="font-semibold text-dark-800">Configuration SMTP actuelle</p>
    <p>Host : {{ mailerHost }}</p>
    <p>Port : {{ mailerPort }}</p>
    <p>Encryption : {{ mailerEncryption }}</p>
    <p>Username : {{ mailerUsername }}</p>
    <p>Password : {% if mailerPassword %}****** (configure){% else %}<span class="text-red-600 font-medium">NON CONFIGURE</span>{% endif %}</p>
  </div>
</div>
{% endblock %}
